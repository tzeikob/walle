conky.config = {
    alignment = 'bottom_right',
    xinerama_head = 0,
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'white',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,
    extra_newline = false,
    font = 'DejaVu Sans Mono:size=12',
    gap_x = 60,
    gap_y = 60,
    minimum_height = 5,
    minimum_width = 5,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_x = true,
    own_window = true,
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_class = 'Conky',
    own_window_type = 'desktop',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 1.0,
    uppercase = false,
    use_spacer = 'none',
    use_xft = true,
    lua_load = '~/.config/#PKG_NAME/main.lua',
    lua_draw_hook_post = 'main'
}

conky.text = [[
${lua eval color mode}
${lua line head}${time %H:%M}${lua end}
${lua line}${lua var month_name}${time %e, %Y}${lua end}
${lua line}Logged as $user_names in $nodename${lua end}
${lua line}Running ${lua var rls_name} ${lua var rls_codename} v${lua var rls_version}${lua end}
${lua line}Process $cpu% Mem $memperc% Disk ${fs_used_perc /}%${lua end}
${lua line}Up ${lua eval upspeedf net_name}KiB/s Down ${lua eval downspeedf net_name}KiB/s${lua end}
${if_empty ${lua var net_name}}\
    ${lua line}Connection is down!${lua end}\
${else}\
    ${lua line}Local Address ${lua var net_ip}${lua end}
    ${lua line}Public Address ${lua var public_ip}${lua end}\
${endif}
${lua line}System is up for $uptime_short${lua end}
]]